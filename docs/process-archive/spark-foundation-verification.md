# Spark Focused Verification — Foundation Frozen Findings

| ID | Result (`CONFIRMED` or `STILL_BROKEN`) | Exact evidence | Required next action |
|---|---|---|---|
| SPK-FND-001 | STILL_BROKEN | In [types.ts](/home/afifah/worktrees/grounded-route-foundation-spark-verify/src/contracts/types.ts#L186), `AgentPort.stageMapping`/`createDraft` still require a caller-supplied `scenarioMappings: ScenarioImpactMapping[]`. In [actions.ts](/home/afifah/worktrees/grounded-route-foundation-spark-verify/src/domain/actions.ts#L118-L123) and [actions.ts](/home/afifah/worktrees/grounded-route-foundation-spark-verify/src/domain/actions.ts#L170-L174), allowlisting is done only against that provided array, not against an immutable fixture/context source. So a caller can pass a forged allowlist object containing `sc-01` (or other synthetic IDs) and have it treated as valid. Existing SPK-FND-001 regression tests in [actions.test.ts](/home/afifah/worktrees/grounded-route-foundation-spark-verify/tests/domain/actions.test.ts#L231) exercise trusted fixture data only and do not prove trust of the argument source. | Remove `scenarioMappings` from action inputs and load vetted mapping IDs internally from immutable fixture context (or a verifier-bound context token) before mapping mutations. Validate both scenario binding and provenance in-domain, not via caller arguments. |
| SPK-FND-002 | CONFIRMED | The exposed agent interface contains no approval/export/copy/download methods: [types.ts](/home/afifah/worktrees/grounded-route-foundation-spark-verify/src/contracts/types.ts#L186-L205) and [actions.ts](/home/afifah/worktrees/grounded-route-foundation-spark-verify/src/domain/actions.ts#L244-L253) show `agentPort` only has reversible/read actions. Approval/export are only on [residentPort](/home/afifah/worktrees/grounded-route-foundation-spark-verify/src/domain/actions.ts#L255-L258), and `residentRequestExport` requires a live approval snapshot with exact revision checks and no `humanConfirmed` boolean ([actions.ts](/home/afifah/worktrees/grounded-route-foundation-spark-verify/src/domain/actions.ts#L224-L241)). | No blocking action for this finding; keep this port split and continue using only the resident path for `approveDraft`/`requestExport`, with any non-UI caller restricted to `agentPort` only. |

## Terminal result
BLOCKED
